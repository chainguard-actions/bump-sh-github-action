openapi: "3.0.0"
info:
  title: Test API
  version: "1.0.0"
  description: A minimal test API for CI testing
paths:
  /hello:
    get:
      summary: Say hello
      responses:
        "200":
          description: Successful response
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Hello, world!
